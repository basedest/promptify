This directory is intentionally kept as an empty placeholder.

We use `src/pages` as the Feature-Sliced Design (FSD) pages layer. Having a
top-level `pages/` folder prevents accidental use of the legacy Next.js Pages
Router and keeps the naming consistent for the FSD layer.
